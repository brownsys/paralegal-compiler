{
let {{{sink}}}_callsite = {{{sink}}}.siblings(&ctx);

CauseBuilder::new("{{span}}", "{{clause_num}}")
    .goes_to(
        {{{src}}},
        {{{sink}}}.extended({{{sink}}}_callsite),
        ctx.as_ref()
    )
}