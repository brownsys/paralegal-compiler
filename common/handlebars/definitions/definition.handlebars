{{#if node-cluster}}
{{{intro}}}
{{else}}
let mut {{{var}}} = {{{intro}}};
{{/if}}
{{#if filter}}
    .filter(|n| {
        let {{{inner_var}}} = *n;
        (|| { {{{filter}}} })().0
    })
{{/if}}
{{#if node-cluster}}
)
{{/if}}
;